---
layout: layouts/base.njk
title: Design Styles Gallery
description: Browse our collection of authentic design style demonstrations showcasing historical and contemporary design movements
---

<div class="hero">
  <div class="container">
    <h1>Design Styles Gallery</h1>
    <p class="hero-description">
      Explore our curated collection of design style demonstrations, each faithfully recreating the principles and aesthetics of iconic design movements.
    </p>
  </div>
</div>

<section class="container">
  {% if designStyles.length > 0 %}
    <div class="grid grid-3">
      {% for style in designStyles %}
        <a href="/gallery/{{ style.slug.current }}/" class="card">
          {% if style.sampleImages and style.sampleImages.length > 0 %}
            <img
              src="{{ style.sampleImages[0].asset._ref | replace('image-', 'https://cdn.sanity.io/images/' + site.sanityProjectId + '/' + site.sanityDataset + '/') | replace('-jpg', '.jpg') | replace('-png', '.png') }}"
              alt="{{ style.sampleImages[0].alt or style.title }}"
              class="card-image"
              loading="lazy"
            />
          {% else %}
            <div class="card-image" style="background: linear-gradient(135deg, #000 0%, #666 100%);"></div>
          {% endif %}

          <div class="card-content">
            <h3 class="card-title">{{ style.title }}</h3>
            <p class="card-description">{{ style.description }}</p>

            {% if style.demoUrl %}
              <div class="mt-md">
                <span class="button button-primary">View Demo</span>
              </div>
            {% endif %}
          </div>
        </a>
      {% endfor %}
    </div>
  {% else %}
    <div class="text-center">
      <p>No design styles available yet. Check back soon!</p>
      <a href="/submit/" class="button button-primary mt-lg">Be the First to Submit</a>
    </div>
  {% endif %}
</section>
